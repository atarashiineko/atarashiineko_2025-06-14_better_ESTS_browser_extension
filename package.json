{
  "name": "identifier-tracker-extension",
  "version": "1.0.0",
  "description": "Browser extension to track and highlight identifiers using configurable regexes.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vite-plugin-static-copy": "^3.0.0"
  },
  "dependencies": {
    "dexie": "^3.2.4"
  }
}
